# openspec/config.yaml — Generated by sdd-init
# Describes the project context for SDD sub-agents

project:
  name: "train64-demo"
  description: "Chess training platform — Train64"

stack:
  languages:
    - "TypeScript"
  frameworks:
    - "Next.js 16.2.1"
    - "React 19.2.4"
    - "Tailwind CSS 4"
  test_frameworks:
    - "unknown"  # No test framework detected in package.json
  build_tools:
    - "Next.js (built-in)"
    - "pnpm"
    - "PostCSS"

architecture:
  pattern: "Next.js App Router (single project)"
  entry_points:
    - "app/page.tsx"
    - "app/layout.tsx"

conventions:
  commit_style: "conventional commits"
  linting: "eslint (eslint-config-next/core-web-vitals + typescript)"
  formatting: "unknown"  # No prettier or formatter detected

# SDD behavior configuration
sdd:
  tdd: false
  detail_level: standard
